<Window x:Class="PTProject.Presentation.Views.CollectionMasterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PTProject.Presentation.Views"
        mc:Ignorable="d"
        Title="CollectionMasterView" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <ListView Grid.Column="0" ItemsSource="{Binding Users}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="GoodsID" Width="60" DisplayMemberBinding="{Binding UserId}" />
                    <GridViewColumn Header="Good Name" Width="70" DisplayMemberBinding="{Binding UserName}" />
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Grid.Column="1" Orientation="Vertical">
            <Label Content="Good Name" />
            <TextBox Text="{Binding NewUserName, UpdateSourceTrigger=PropertyChanged}" />
            <Button Command="{Binding AddUserCommand}" CommandParameter="{Binding NewUserName}" Content="Add Good" />
        </StackPanel>
    </Grid>
</Window>
