<!-- UserMasterView.xaml -->
<Window x:Class="PTProject.Presentation.Views.UserMasterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="UserMasterView" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <ListView Grid.Column="0" ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}" SelectionChanged="ListView_SelectionChanged">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="User ID" DisplayMemberBinding="{Binding UserId}" />
                    <GridViewColumn Header="User Name" DisplayMemberBinding="{Binding UserName}" />
                </GridView>
            </ListView.View>
        </ListView>

        <ContentControl Grid.Column="1" Content="{Binding DetailView}" />
        <StackPanel Grid.Column="1" Orientation="Vertical">
            <Label Content="User Name" />
            <TextBox Text="{Binding NewUserName, UpdateSourceTrigger=PropertyChanged}" />
            <Button Command="{Binding AddUserCommand}" CommandParameter="{Binding NewUserName}" Content="Add User" />
        </StackPanel>
    </Grid>
</Window>
